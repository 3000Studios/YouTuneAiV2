# YouTuneAI Custom Redirect
# Force main domain to show our working HTML
DirectoryIndex index.html index.php

# If WordPress isn't working, redirect to our HTML
RewriteEngine On
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^$ /wp-content/themes/wp-theme-youtuneai/index.html [R=302,L]
